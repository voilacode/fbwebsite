# API Configuration
# In development, Vite proxy handles API requests (see vite.config.ts)
# In production, set the full URL to your Laravel backend
VITE_API_URL=/api/website

# App Configuration
VITE_APP_NAME=VoilaCode
